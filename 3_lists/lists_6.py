#a list of numbers
numbers = [10,11,12,13,14,15]

l = len(numbers)

#passing variables into a list
print(numbers[l-1])


#passing variables into a list
#retuns the first element in the list
#numbers[2-2]
